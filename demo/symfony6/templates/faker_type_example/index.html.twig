{% extends 'base.html.twig' %}

{% block title %}Faker Type Example - {{ connection|upper }}{% endblock %}

{% block body %}
{% include '_breadcrumbs.html.twig' with {entityName: 'Faker Type Example', connection: connection, routePrefix: 'faker_type_example', currentPage: 'List'} %}

{% if not hasAnonymizedColumn|default(false) %}
<div class="alert alert-warning" role="alert">
    <strong>⚠️ Column "anonymized" not found</strong><br>
    To add the anonymization tracking column, run:<br>
    <code class="d-inline-block mt-2 p-2 bg-light rounded">
        php bin/console nowo:anonymize:generate-column-migration --connection {{ connection }}
    </code>
</div>
{% endif %}

<div class="card shadow-sm">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h2 class="mb-0">Faker Type Example <span class="badge bg-{{ connection == 'default' ? 'success' : 'primary' }} connection-badge">{{ connection }}</span></h2>
        <a href="{{ path('faker_type_example_new', {connection: connection}) }}" class="btn btn-primary">Create New Faker Type Example</a>
    </div>
    <div class="card-body">
        {% if fakertypeexample_list|length > 0 %}
            <div class="table-responsive">
                <table class="table table-hover">
                    <thead class="table-light">
                        <tr>
                            <th>ID</th>
                            {% for field in fakertypeexample_list[0]|keys %}
                                {% if field != 'id' and field != 'anonymized' %}
                                <th>{{ field|replace({'_': ' '})|title }}</th>
                                {% endif %}
                            {% endfor %}
                            {% if hasAnonymizedColumn|default(false) %}
                            <th>Anonymized</th>
                            {% endif %}
                            <th>Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        {% for fakertypeexample in fakertypeexample_list %}
                            <tr>
                                <td>{{ fakertypeexample.id ?? fakertypeexample.id }}</td>
                                {% for field in fakertypeexample_list[0]|keys %}
                                    {% if field != 'id' and field != 'anonymized' %}
                                    <td>{{ fakertypeexample[field] ?? '-' }}</td>
                                    {% endif %}
                                {% endfor %}
                                {% if hasAnonymizedColumn|default(false) %}
                                <td>
                                    {% if fakertypeexample.anonymized|default(false) %}
                                    {% include '_anonymized_badge.html.twig' with {value: fakertypeexample.anonymized|default(false)} %}
                                    {% endif %}
                                </td>
                                {% endif %}
                                <td>
                                    <div class="btn-group" role="group">
                                        <a href="{{ path('faker_type_example_show', {connection: connection, id: fakertypeexample.id ?? fakertypeexample.id}) }}" class="btn btn-sm btn-outline-secondary">Show</a>
                                        <a href="{{ path('faker_type_example_edit', {connection: connection, id: fakertypeexample.id ?? fakertypeexample.id}) }}" class="btn btn-sm btn-outline-primary">Edit</a>
                                        <form method="post" action="{{ path('faker_type_example_delete', {connection: connection, id: fakertypeexample.id ?? fakertypeexample.id}) }}" class="d-inline" onsubmit="return confirm('Are you sure?')">
                                            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ (fakertypeexample.id ?? fakertypeexample.id)) }}">
                                            <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                        </form>
                                    </div>
                                </td>
                            </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        {% else %}
            <p class="text-muted">No fakertypeexample found. <a href="{{ path('faker_type_example_new', {connection: connection}) }}">Create the first one</a>.</p>
        {% endif %}
    </div>
</div>
{% endblock %}

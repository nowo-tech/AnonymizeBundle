{% extends 'base.html.twig' %}

{% block title %}User Account Details - {{ connection|upper }}{% endblock %}

{% block body %}
{% include '_breadcrumbs.html.twig' with {entityName: 'User Account', connection: connection, routePrefix: 'user_account', currentPage: 'Show'} %}

<div class="card shadow-sm">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h2 class="mb-0">User Account Details <span class="badge bg-{{ connection == 'default' ? 'success' : 'primary' }} connection-badge">{{ connection }}</span></h2>
        <div class="btn-group">
            <a href="{{ path('user_account_edit', {connection: connection, id: useraccount.id}) }}" class="btn btn-primary">Edit</a>
            <a href="{{ path('user_account_index', {connection: connection}) }}" class="btn btn-secondary">Back to List</a>
        </div>
    </div>
    <div class="card-body">
        <dl class="row">
            <dt class="col-sm-3">ID</dt>
            <dd class="col-sm-9">{{ useraccount.id }}</dd>
            {% for field in useraccount|keys %}
                {% if field != 'id' and field != 'anonymized' %}
                <dt class="col-sm-3">{{ field|replace({'_': ' '})|title }}</dt>
                <dd class="col-sm-9">{{ useraccount[field] ?? '-' }}</dd>
                {% endif %}
            {% endfor %}
            {% if hasAnonymizedColumn|default(false) %}
            <dt class="col-sm-3">Anonymized</dt>
            <dd class="col-sm-9">
                {% if useraccount.anonymized|default(false) %}
                {% include '_anonymized_badge.html.twig' with {value: useraccount.anonymized|default(false)} %}
                {% endif %}
            </dd>
            {% endif %}
        </dl>

        <div class="mt-4">
            <form method="post" action="{{ path('user_account_delete', {connection: connection, id: useraccount.id}) }}" class="d-inline" onsubmit="return confirm('Are you sure you want to delete this useraccount?')">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ useraccount.id) }}">
                <button type="submit" class="btn btn-danger">Delete User Account</button>
            </form>
        </div>
    </div>
</div>
{% endblock %}
